let arr = ['Speaker', 'Monitor', 'Keyboard', 'Mouse', 'Printer'];
for (var i = 0; i < arr.length; i++) {
    if (arr[i].includes('a')) {
        console.log(arr[i]);
    }
}

// Output
// Speaker
// Keyboard
